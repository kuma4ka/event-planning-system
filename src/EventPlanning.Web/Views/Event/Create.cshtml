@using EventPlanning.Domain.Enums
@model CreateEventDto

@{
    ViewData["Title"] = "Create Event";

    var eventTypes = Enum.GetValues<EventType>().Select(e => new SelectListItem
            {
                Value = e.ToString(),
                Text = e.GetDisplayName()
            });
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="mb-4">
                <a asp-action="MyEvents" class="text-decoration-none text-muted small hover-effect">
                    <i class="bi bi-arrow-left me-1"></i> Back to Dashboard
                </a>
            </div>

            <div class="st-card animate-fade-in">
                <div class="text-center mb-5">
                    <div class="d-inline-flex align-items-center justify-content-center bg-light rounded-circle mb-3"
                        style="width: 60px; height: 60px; color: var(--color-dark-teal);">
                        <i class="bi bi-calendar-plus fs-3"></i>
                    </div>
                    <h2 class="st-form-title">Create New Event</h2>
                    <p class="text-muted">Host your next gathering with us</p>
                </div>

                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 shadow-sm mb-4 small">
                    </div>

                    <partial name="Partials/_EventFormFields" model="Model" />

                    <div class="st-form-actions">
                        <a asp-action="Index" asp-controller="Home" class="btn btn-outline-secondary px-4">Cancel</a>
                        <button type="submit" class="btn btn-primary px-5 shadow-sm">
                            Create Event <i class="bi bi-check-lg ms-2"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}